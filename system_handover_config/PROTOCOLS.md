# Runtime Protocols

- Use absolute paths.
- Keep the active prompt window limited to the current job packet.
- Suppress historical logs and review artifacts during standard application runs.
- Load interview libraries only when interview mode is explicitly triggered.
- Apply immediately after fast-path tailoring when `stack_match >= 0.70`.
- Do not add reflection, verification, or double-check steps.
